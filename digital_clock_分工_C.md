# 工程师 C — 核心时序逻辑工程师

## 职责
主 always 块所有时序逻辑：复位初始化、闹钟触发、报时消除、按键调整（设置时间/闹钟）、正常自动计时累加。

## 负责代码

### 蜂鸣器驱动

| 行号 | 代码 |
|------|------|
| 51 | `always @(posedge clk or posedge rst) begin` |
| 52 | `if(rst) buzzer <= 1'b0;` |
| 53 | `else    buzzer <= (alm && (key_mode == S_NORMAL)) ? 1'b1 : 1'b0;` |
| 54 | `end` |

### 主时序块

| 行号 | 代码 |
|------|------|
| 59 | `always @(posedge clk or posedge rst) begin` |
| 60 | `if(rst) begin` |
| 61-64 | 初始时间 12:00:00 |
| 65-67 | 初始闹钟 07:00 |
| 68 | `alm <= 1'b0;` |
| 69 | `end else begin` |
| 71-79 | 闹钟/整点触发判断（00秒触发，匹配时+整点双条件 OR） |
| 80-84 | 报时消除（响一个周期后自动关闭） |
| 86-108 | 按键调整逻辑（key_add 上升沿 → 调时/调分/调闹钟时/调闹钟分） |
| 110-126 | 正常自动计时（秒→分→时层叠进位，BCD 循环：秒59→00、分59→00、时23→00） |
| 127-129 | always 块结束 |

### 核心逻辑详解

1. **闹钟触发** (L72-78): `key_mode==NORMAL && (s==00) && !alm` 时，若时钟匹配闹钟或分钟为整点，拉高 alm
2. **报时消除** (L81-84): alm 为高时下一周期自动清零，只响一个时钟周期
3. **按键调整** (L86-108): ke 上升沿有效，根据 key_mode 切换 S_SET_TIME / S_SET_ALARM，k3 选择调节"时"或"分"，BCD 边界回绕
4. **自动计时** (L110-126): 层叠 if-else 结构实现 59秒→00秒、59分→00分、23时→00时的 BCD 进位

## 工作量
- **行数**: ~72 行
- **占比**: ~47%（代码主体）
- **关键贡献**: 整个数字钟的计时引擎、闹钟逻辑、按键调整状态机
