# digital_clock.v 项目逐行解读

> 基于 BUPT TEC-8 实验箱的 Verilog 数字时钟系统  
> 四人分工：逐行标注每一行代码的责任人

---

## 四人分工总览

| 角色 | 人 | 职责范围 | 代码行数 |
|------|----|---------|---------|
| **模块架构师 A** | 甲 | 顶层模块声明、端口定义、参数常量、寄存器变量声明、连线声明 | 第 1–31 行 |
| **信号处理师 B** | 乙 | 按键消抖边缘检测、闪烁信号生成、蜂鸣器驱动 | 第 33–48 行 |
| **核心逻辑师 C** | 丙 | 主时序 always 块：计时累加、闹钟触发/消除、按键调时/调闹钟 | 第 52–127 行 |
| **显示驱动师 D** | 丁 | 显示多路选择器、输出端口赋值、闪烁效果、七段译码函数 | 第 130–176 行 |

---

## 【甲 — 模块架构师】第 1–31 行

### 第 1 行：模块声明头
```verilog
module digital_clock(
```
- **责任人：甲**
- **说明：** Verilog 模块定义的起始。模块名 `digital_clock`，与文件名一致。TEC-8 实验箱通过此模块名在顶层原理图中实例化。

### 第 2 行：系统时钟输入
```verilog
    input wire clk,             // 系统时钟（建议接入 1Hz 用于计时，若是高频时钟需在外部先分频）
```
- **责任人：甲**
- **说明：** 声明一个名为 `clk` 的输入型 `wire` 类型端口。这是整个时钟系统的基准节拍。
- **设计决策：** 注释明确建议接 1Hz 信号。如果 TEC-8 实验箱只能提供更高频率（如 50MHz），需要在外部（顶层模块或专门的时钟分频模块）先分频到 1Hz 再接入。因为核心计时逻辑直接在每个 `posedge clk` 累加秒个位，若时钟不是 1Hz，一秒内会触发多次累加，计时会快很多倍。
- **TEC-8 映射：** 通常接实验箱的时钟源输出，经分频器（如 50_000_000 → 1）后接入。

### 第 3 行：复位信号
```verilog
    input wire rst,
```
- **责任人：甲**
- **说明：** 异步复位输入，高电平有效。当 `rst=1` 时，所有寄存器立即回到初始值（12:00:00，闹钟 07:00）。
- **TEC-8 映射：** 接实验箱的全局复位按键，通常标记为 "RST" 或 "CLR"。

### 第 4 行：模式选择输入
```verilog
    input wire [1:0] key_mode,  // 0:正常运行, 1:设置时间, 2:设置闹钟
```
- **责任人：甲**
- **说明：** 2 位宽输入，三个有效值：`2'b00`=正常走时，`2'b01`=调时模式，`2'b10`=调闹钟模式。`2'b11` 为未定义状态，在 case 中由 default 分支处理。
- **设计决策：** 用 2 位总线而非三个独立按键，节省 TEC-8 实验箱宝贵的 I/O 引脚。由外部拨码开关或按键编码电路产生。

### 第 5 行：数值增加按键
```verilog
    input wire key_add,         // 调整数值的按键
```
- **责任人：甲**
- **说明：** 在调时/调闹钟模式下，每次按下此键，被选中的时间位（时或分）加 1。需要配合 `k3` 决定是调时还是调分。
- **设计决策：** 不含硬件消抖，由内部 `key_prev` 寄存器实现软件消抖（边缘检测）。

### 第 6 行：时/分选择按键
```verilog
    input wire k3,              // 0:选择调节"时", 1:选择调节"分"
```
- **责任人：甲**
- **说明：** 在设置模式下，选择当前要调整的单元：
  - `k3=0` → 调节"小时"（时十位/时个位循环）
  - `k3=1` → 调节"分钟"（分十位/分个位循环）

### 第 7–12 行：输出端口
```verilog
    output reg [3:0] hour_ten,
    output reg [3:0] hour_one,
    output reg [3:0] min_ten,
    output reg [3:0] min_one,
    output reg [3:0] sec_ten,
    output reg [6:0] sec_one,   // 保持原样，仅秒个位输出七段码
    output reg buzzer
);
```
- **责任人：甲**
- **说明：** 七个输出端口：
  - `hour_ten[3:0]` — 小时的十位（BCD：0-2）→ TEC-8 数码管
  - `hour_one[3:0]` — 小时的个位（BCD：0-9）
  - `min_ten[3:0]` — 分钟的十位（BCD：0-5）
  - `min_one[3:0]` — 分钟的个位（BCD：0-9）
  - `sec_ten[3:0]` — 秒的十位（BCD：0-5）
  - `sec_one[6:0]` — **秒个位的七段码**（已译码），7 位总线直接驱动数码管
  - `buzzer` — 蜂鸣器输出，闹钟触发时拉高
- **设计决策：** 为什么只有 `sec_one` 输出七段码？因为 TEC-8 实验箱的数码管段选线有限，为了节省资源，其他五位（时十位/个位、分十位/个位、秒十位）期望在实验箱的数码管驱动端自行做 BCD→七段译码，或者共用译码器。只有秒个位在这里提前译码，可能是为了展示七段译码模块作为演示。
- **类型声明为 `reg`**：因为这些信号在 `always` 块中被赋值。

### 第 14 行：状态机常量
```verilog
localparam S_NORMAL=0, S_SET_TIME=1, S_SET_ALARM=2;
```
- **责任人：甲**
- **说明：** 用 `localparam` 定义三个状态常量：
  - `S_NORMAL = 2'd0` — 正常走时模式
  - `S_SET_TIME = 2'd1` — 设置时间模式
  - `S_SET_ALARM = 2'd2` — 设置闹钟模式
- **作用域：** `localparam` 仅在当前模块内可见，不同于 `parameter` 不可被外部覆盖。确保模式值不会被上层意外修改。

### 第 18–28 行：寄存器声明
```verilog
// =========================================================================
// 核心寄存器：全部改为 BCD 码存储（个位和十位拆开），彻底抹除运行时拆分逻辑
// =========================================================================
reg [3:0] s_l, s_h;             // 秒个位(0-9)，秒十位(0-5)
reg [3:0] m_l, m_h;             // 分个位(0-9)，分十位(0-5)
reg [3:0] h_l, h_h;             // 时个位(0-9)，时十位(0-2)

reg [3:0] am_l, am_h;           // 闹钟：分个位，分十位
reg [3:0] ah_l, ah_h;           // 闹钟：时个位，时十位

reg key_prev;
reg blk;
reg alm;
```
- **责任人：甲**
- **说明：** 全部寄存器采用 **BCD 码拆分存储**（Binary-Coded Decimal，每位十进制数用 4 位二进制表示）：
  - `s_l` / `s_h` — 秒个位（值域 0-9）/ 秒十位（值域 0-5），组合为 00–59
  - `m_l` / `m_h` — 分个位（0-9）/ 分十位（0-5），组合为 00–59
  - `h_l` / `h_h` — 时个位（0-9）/ 时十位（0-2），组合为 00–23
  - `am_l` / `am_h` — 闹钟分钟个位/十位
  - `ah_l` / `ah_h` — 闹钟小时个位/十位
  - `key_prev` — 按键前一个时钟周期的值，用于边缘检测
  - `blk` — 闪烁控制，每时钟周期翻转一次
  - `alm` — 闹钟触发标志，1=响铃中
- **设计决策：** "彻底抹除运行时拆分逻辑"——传统做法是存一个二进制整数再做 `mod 10` 和 `/ 10` 拆成个位十位，需要额外组合逻辑。这里直接存拆好的 BCD 位，计时进位时逐位判断，省掉了取模和除法运算，时序更紧凑、资源更省。

---

## 【乙 — 信号处理师】第 33–48 行

### 第 36–40 行：按键边缘检测
```verilog
// 按键去抖/消抖边缘检测
always @(posedge clk or posedge rst) begin
    if(rst) key_prev <= 1'b0;
    else key_prev <= key_add;
end
assign ke = (key_add && !key_prev); // 调整为标准的上升沿触发
```
- **责任人：乙**
- **说明：** 这是一个**单级寄存器消抖**（单级同步器）实现按键上升沿检测：
  - **always 块（第 37–39 行）：** 每个时钟上升沿（或复位），将 `key_add` 当前值存入 `key_prev`。这样 `key_prev` 始终保存的是上一个时钟周期时按键的值。
  - **assign 语句（第 40 行）：** 组合逻辑。`ke = key_add && !key_prev`：
    - 当按键刚被按下时：`key_add=1`（当前高），`key_prev=0`（上一个周期还是低）→ `ke=1`（检测到上升沿）
    - 当按键持续按住时：`key_add=1`，`key_prev=1` → `ke=0`（只触发一次）
    - 按键释放时：`key_add=0` → `ke=0`
  - **"上升沿触发"：** 注释提到"调整为标准的上升沿触发"，意味着每按一次按键只产生一个时钟周期宽度的 `ke` 脉冲，避免了按住不放时重复触发。
  - **局限：** 单级寄存器在按键机械抖动严重时可能误触发。改进方案是加一个计数消抖（连续 N 个周期采样一致才判定按下）。但对于 TEC-8 实验箱的 1Hz 低频时钟，机械抖动已经过去，单级足够。

### 第 42–44 行：闪烁信号发生器
```verilog
// 闪烁信号 (若 clk 为 1Hz，则每秒翻转一次)
always @(posedge clk or posedge rst) begin
    if(rst) blk <= 1'b0;
    else blk <= ~blk;
end
```
- **责任人：乙**
- **说明：** 每个时钟周期 `blk` 翻转一次（取反）：
  - **复位时：** `blk = 0`
  - **正常时：** `blk <= ~blk` — 每来一个 `posedge clk`，`blk` 在 0 和 1 之间切换
  - **视觉效果：** 如果 `clk` 是 1Hz，那么 `blk` 以 0.5Hz（每秒一次）的频率在 0/1 之间切换。这在设置模式下配合显示逻辑，让被选中的数字每隔一秒消失一次，形成**视觉闪烁**，提示用户当前正在调节的是哪个字段。
  - **资源消耗：** 仅 1 个触发器 + 1 个反相器，几乎零成本。

### 第 46–48 行：蜂鸣器驱动
```verilog
// 蜂鸣器驱动：只要闹钟标志 alm 为 1，且处于正常走时模式，就直接输出高电平响铃
always @(posedge clk or posedge rst) begin
    if(rst) buzzer <= 1'b0;
    else    buzzer <= (alm && (key_mode == S_NORMAL)) ? 1'b1 : 1'b0;
end
```
- **责任人：乙**
- **说明：** 蜂鸣器输出的组合/时序混合逻辑：
  - **复位行为：** `buzzer <= 1'b0` — 复位时强制静音
  - **正常行为：** 三目运算符 `(alm && key_mode==S_NORMAL) ? 1 : 0`
    - `alm=1`（闹钟触发）**且** `key_mode==S_NORMAL`（处于正常走时模式）→ `buzzer=1`（响铃）
    - 其他情况 → `buzzer=0`（静音）
  - **设计意图：** 为什么需要 `key_mode==S_NORMAL` 条件？因为当用户进入设置模式调闹钟时，不应该响铃（此时虽然 `alm` 可能还是 1，但蜂鸣器被静音）。同时，闹钟触发的确切时刻是时钟走到闹钟设定的时间那一秒。
  - **输出特性：** 这里输出的是持续高电平，不是 PWM 方波。TEC-8 实验箱的蜂鸣器驱动是高电平有效，持续拉高即连续发声。如果期望哔哔的断续声，外部或此处需要加一个方波发生器。

---

## 【丙 — 核心逻辑师】第 52–127 行

### 第 52 行：主时序 always 块开始
```verilog
// =========================================================================
// 主时序逻辑：计时 + 调整（由于采用 BCD 架构，所有逻辑变为纯粹的计数器使能）
// =========================================================================
always @(posedge clk or posedge rst) begin
```
- **责任人：丙**
- **说明：** 这是整个时钟的核心，一个大的同步时序 always 块，对 `clk` 上升沿和 `rst` 上升沿敏感。包含了：复位初始化、闹钟触发、闹钟消除、按键调整、自动计时五个子功能。
- **"计数器使能"：** BCD 架构下，不再需要二进制加法和除/模运算。每一位的进位条件变成简单的"是否等于 9 或 5 或 3"的比较，逻辑清晰。

### 第 53–61 行：复位初始化
```verilog
    if(rst) begin
        // 初始时间 12:00:00
        h_h <= 4'd1; h_l <= 4'd2;
        m_h <= 4'd0; m_l <= 4'd0;
        s_h <= 4'd0; s_l <= 4'd0;
        // 初始闹钟 07:00
        ah_h <= 4'd0; ah_l <= 4'd7;
        am_h <= 4'd0; am_l <= 4'd0;
        alm <= 1'b0;
    end else begin
```
- **责任人：丙**
- **说明：** 异步复位时设置所有寄存器的初值：
  - **当前时间：** `12:00:00`
    - `h_h=1, h_l=2` → 时 = 12
    - `m_h=0, m_l=0` → 分 = 00
    - `s_h=0, s_l=0` → 秒 = 00
  - **闹钟时间：** `07:00`
    - `ah_h=0, ah_l=7` → 时 = 07
    - `am_h=0, am_l=0` → 分 = 00
  - **闹钟标志：** `alm = 0`（关闭状态）
- **设计决策：** 初始时间 12:00:00 而非 00:00:00。可能是为了让上电时数码管显示非零值，便于验证显示驱动是否正确。闹钟设为 07:00（早上 7 点），是典型的起床时间。

### 第 63–67 行：闹钟触发判断
```verilog
        // 1. 闹钟触发判断 (必须严格卡在 00 秒触发，防止关闭后在同一分钟内反复诈尸)
        if ((key_mode == S_NORMAL) && 
            ({h_h, h_l, m_h, m_l} == {ah_h, ah_l, am_h, am_l}) && 
            (s_h == 4'd0 && s_l == 4'd0) && (!alm)) begin
            
            alm <= 1'b1; 
        end
```
- **责任人：丙**
- **说明：** 闹钟触发条件（四个条件**必须同时满足**）：
  1. `key_mode == S_NORMAL` — 处于正常走时模式（设置模式下不触发闹钟）
  2. `{h_h, h_l, m_h, m_l} == {ah_h, ah_l, am_h, am_l}` — 当前时间的小时和分钟**完全等于**闹钟设定值。这里用位拼接运算符 `{}` 把两组 4 位 BCD 码拼成 16 位总线后做全等比较。
  3. `s_h==0 && s_l==0` — **秒必须为 00**。这是"卡在 00 秒触发"的关键：只在分钟切换的那一秒触发，不会在该分钟内的其他秒数重复触发。
  4. `!alm` — 闹钟尚未触发，防止重复触发。
- **"防止诈尸"：** 注释中"防止关闭后在同一分钟内反复诈尸"指的是：如果用户在闹钟响后手动关闭（`alm=0`），但此时分钟还没变（比如 07:00:15，分仍然是 00），如果没有 `!alm` 条件且 `s_h==0 && s_l==0` 的条件在秒循环一圈回到 00 时再次满足，闹钟会重新触发。加上 `!alm` 后，关闭后至少要到下一分钟才能再次触发。

### 第 68–76 行：闹钟消除逻辑
```verilog
        // 2. 闹钟消除逻辑
        else if (alm) begin
            // 响满 30 秒自动关闭
            if (s_h == 4'd3 && s_l == 4'd0) begin
                alm <= 1'b0;
            end
            // 手动关闭闹钟
            else if (ke && (key_mode == S_NORMAL)) begin
                alm <= 1'b0;
            end
        end
```
- **责任人：丙**
- **说明：** 闹钟响铃后有两种方式关闭：
  1. **自动关闭（第 71–73 行）：** `s_h == 3 && s_l == 0` 即秒数到达 30 秒时自动关停。从闹钟触发时（秒=00）开始数 30 个时钟周期（30 秒）后自动静音。
  2. **手动关闭（第 75–77 行）：** 检测到按键上升沿 `ke=1` 且处于正常模式时立即关闭。
- **为什么用 `else if` 而不是 `if`：** 与前面的闹钟触发 `if` 是互斥关系。当 `alm=0`（未触发）时进入触发判断；当 `alm=1`（已触发）时进入消除逻辑。永远不会同时执行。

### 第 78–95 行：按键调整逻辑
```verilog
        // 2. 按键调整逻辑
        if(ke) begin
            if(key_mode == S_SET_TIME) begin
                if(k3 == 1'b0) begin // 调时
                    if(h_h == 4'd2 && h_l == 4'd3) begin h_h <= 4'd0; h_l <= 4'd0; end
                    else if(h_l == 4'd9)           begin h_l <= 4'd0; h_h <= h_h + 1'b1; end
                    else                           h_l <= h_l + 1'b1;
                end else begin // 调分
                    if(m_h == 4'd5 && m_l == 4'd9) begin m_h <= 4'd0; m_l <= 4'd0; end
                    else if(m_l == 4'd9)           begin m_l <= 4'd0; m_h <= m_h + 1'b1; end
                    else                           m_l <= m_l + 1'b1;
                end
            end else if(key_mode == S_SET_ALARM) begin
                if(k3 == 1'b0) begin // 调闹钟时
                    if(ah_h == 4'd2 && ah_l == 4'd3) begin ah_h <= 4'd0; ah_l <= 4'd0; end
                    else if(ah_l == 4'd9)            begin ah_l <= 4'd0; ah_h <= ah_h + 1'b1; end
                    else                             ah_l <= ah_l + 1'b1;
                end else begin // 调闹钟分
                    if(am_h == 4'd5 && am_l == 4'd9) begin am_h <= 4'd0; am_l <= 4'd0; end
                    else if(am_l == 4'd9)            begin am_l <= 4'd0; am_h <= am_h + 1'b1; end
                    else                             am_l <= am_l + 1'b1;
                end
            end
        end
```
- **责任人：丙**
- **说明：** 按键 `ke` 上升沿触发时的调整逻辑。这是一个**级联条件树**：

  **外层 `if(ke)` — 只有按键事件才进入**

  **第一分支：`key_mode == S_SET_TIME`（调时模式）**
  - **`k3==0` — 调小时：**
    - `h_h==2 && h_l==3`（小时=23）→ 回绕到 `00`（24 小时制）
    - `h_l==9`（小时个位到 9，如 09→10 或 19→20）→ 个位归零，十位加 1
    - 其他情况 → 个位加 1
  - **`k3==1` — 调分钟：**
    - `m_h==5 && m_l==9`（分钟=59）→ 回绕到 `00`
    - `m_l==9`（分钟个位到 9）→ 个位归零，十位加 1
    - 其他情况 → 个位加 1

  **第二分支：`key_mode == S_SET_ALARM`（调闹钟模式）**
  - **`k3==0` — 调闹钟小时：** 逻辑完全同上
  - **`k3==1` — 调闹钟分钟：** 逻辑完全同上

- **设计决策：** 注意调整逻辑**不改变秒和闹钟秒**，也没有分钟调整时的秒归零操作（不像很多电子钟调分时会顺手清 0 秒）。这是一个简化。

### 第 96–116 行：自动计时逻辑
```verilog
        // 3. 正常自动计时逻辑 (只有不在设置时间模式下才向前累加)
        else if(key_mode != S_SET_TIME) begin
            if(s_l == 4'd9) begin
                s_l <= 4'd0;
                if(s_h == 4'd5) begin
                    s_h <= 4'd0;
                    if(m_l == 4'd9) begin
                        m_l <= 4'd0;
                        if(m_h == 4'd5) begin
                            m_h <= 4'd0;
                            // 小时进位判断 (23:59:59 -> 00:00:00)
                            if(h_h == 4'd2 && h_l == 4'd3) begin h_h <= 4'd0; h_l <= 4'd0; end
                            else if(h_l == 4'd9)           begin h_l <= 4'd0; h_h <= h_h + 1'b1; end
                            else                           h_l <= h_l + 1'b1;
                        end else m_h <= m_h + 1'b1;
                    end else m_l <= m_l + 1'b1;
                end else s_h <= s_h + 1'b1;
            end else s_l <= s_l + 1'b1;
        end
    end
end
```
- **责任人：丙**
- **说明：** 这是最核心的**计时链条**，采用嵌套 `if` 实现**逐级进位**。每一级的条件都是"当前位是否已到最大值"：

  **秒个位（s_l）→** 每来一个 `posedge clk` 就加 1（因为 CLK=1Hz）
  - 0→1→2→...→9 → 回到 0，进位到秒十位

  **秒十位（s_h）** → `s_l==9` 时才执行
  - 当 `s_h==5`（即秒=59）：`s_h` 归零，进位到分个位
  - 否则 `s_h++`

  **分个位（m_l）** → `s_h==5 && s_l==9`（秒=59）时执行
  - 0→1→...→9 → 归零，进位到分十位

  **分十位（m_h）** → `m_l==9 && s_h==5 && s_l==9`（秒=59，分个位=9）时执行
  - 当 `m_h==5`（即分=59）：归零，进位到小时
  - 否则 `m_h++`

  **小时个位（h_l）** → `m_h==5 && m_l==9 && s_h==5 && s_l==9`（59分59秒）时执行
  - `h_h==2 && h_l==3`（23:59:59）→ 回绕到 `00:00:00`（24 小时制翻页）
  - `h_l==9`（如 09→10 或 19→20）→ 个位归零，十位加 1
  - 否则 `h_l++`

  **为什么用 `else if` 而不是独立的 `if`：** 按键调整和自动计时是互斥的。当有按键按下时（`ke=1`），走调整逻辑；无按键且不在设置模式时，才走计时逻辑（`key_mode != S_SET_TIME`）。如果在设置模式下，计时停止——时间凝固，用户调整时时间不会偷偷跑走。

### 第 117 行：always 块结束
```verilog
end
```
- **责任人：丙**
- **说明：** 主时序 always 块结束。

---

## 【丁 — 显示驱动师】第 130–176 行

### 第 130–134 行：显示中间寄存器
```verilog
// =========================================================================
// 显示多路选择与输出（纯组合逻辑 MUX，极度节省资源）
// =========================================================================
reg [3:0] dh_h, dh_l;
reg [3:0] dm_h, dm_l;
reg [3:0] ds_h, ds_l;
```
- **责任人：丁**
- **说明：** 声明三个临时总线对，用于在组合逻辑中暂存选中的显示数据：
  - `dh_h / dh_l` — 显示用的小时十位/个位
  - `dm_h / dm_l` — 显示用的分钟十位/个位
  - `ds_h / ds_l` — 显示用的秒十位/个位
- **类型选 `reg`：** 因为在 `always @(*)` 组合逻辑块中赋值必须用 `reg` 类型。但这并不意味着它们会被综合成触发器——组合 always 块综合为纯组合逻辑（MUX 和数据选择器）。

### 第 136 行：组合逻辑块开始
```verilog
always @(*) begin
```
- **责任人：丁**
- **说明：** `always @(*)` 是组合逻辑 always 块（Verilog 2001 风格）。对 `*` 敏感意味着块内所有被读取的信号变化都会触发块重新计算。综合工具会将其综合为纯组合逻辑（无触发器）。

### 第 138–149 行：模式选择 MUX
```verilog
    // 根据当前模式选择送往显示的数据流
    case(key_mode)
        S_SET_TIME: begin
            dh_h = h_h;  dh_l = h_l;
            dm_h = m_h;  dm_l = m_l;
            ds_h = 4'd0; ds_l = 4'd0; // 设置模式下秒清零
        end
        S_SET_ALARM: begin
            dh_h = ah_h; dh_l = ah_l;
            dm_h = am_h; dm_l = am_l;
            ds_h = 4'd0; ds_l = 4'd0;
        end
        default: begin // S_NORMAL
            dh_h = h_h;  dh_l = h_l;
            dm_h = m_h;  dm_l = m_l;
            ds_h = s_h;  ds_l = s_l;
        end
    endcase
```
- **责任人：丁**
- **说明：** 三路选择器，根据 `key_mode` 决定显示哪个数据源：
  - **`S_SET_TIME`（调时模式）：** 显示当前时间（`h_h/h_l`、`m_h/m_l`），**秒强制显示 00**。这是因为调时期间秒本应在冻结状态（自动计时被暂停），显示 00 更直观。
  - **`S_SET_ALARM`（调闹钟模式）：** 显示**闹钟设定值**（`ah_h/ah_l`、`am_h/am_l`），秒也显示 00。这样用户可以查看/调整闹钟时，看到的是闹钟时间而不是当前时间。
  - **`default`（正常模式，`S_NORMAL=0`）：** 显示当前时间的时分秒所有六位。

### 第 151–165 行：输出端口赋值与闪烁
```verilog
    // 最终输出端口赋值与闪烁效果处理
    hour_ten = dh_h;
    
    // 如果在设置模式且当前选中调节"时"(k3==0)，利用 blk 信号让个位显示 0 达到闪烁提示效果
    if((key_mode != S_NORMAL) && (k3 == 1'b0) && blk) hour_one = 4'd0;
    else                                              hour_one = dh_l;

    min_ten = dm_h;
    
    // 如果在设置模式且当前选中调节"分"(k3==1)，利用 blk 信号让个位显示 0 达到闪烁提示效果
    if((key_mode != S_NORMAL) && (k3 == 1'b1) && blk) min_one = 4'd0;
    else                                              min_one = dm_l;

    sec_ten = ds_h;
    sec_one = seg7(ds_l); // 仅在最终输出的一瞬间将秒个位翻译为七段码
end
```
- **责任人：丁**
- **说明：** 将选中的数据显示值映射到输出端口，并实现**闪烁提示**：

  **`hour_ten`（第 151 行）：** 小时十位直接输出，不闪烁。因为两个数码管同时闪烁会让用户困惑，只闪个位足以提示。

  **`hour_one`（第 153–154 行）：**
  - 闪烁条件：`key_mode != S_NORMAL && k3==0 && blk`
    - 不在正常模式（调时或调闹钟）→ 才需要提示
    - `k3==0` → 当前选中小时 → 小时个位闪烁
    - `blk==1` → 闪烁周期的一半时间显示 0（"灭"的效果），另一半正常显示
  - 效果：在调时/调闹钟模式下，如果 `k3=0`（调小时），小时个位每隔 1 秒在正常值和 0 之间切换，看起来在闪烁。

  **`min_ten`（第 156 行）：** 分钟十位直接输出，同理不闪烁。

  **`min_one`（第 158–159 行）：**
  - 闪烁条件：`key_mode != S_NORMAL && k3==1 && blk`
  - 当 `k3=1`（调分钟）时，分钟个位闪烁。与小时闪烁互斥。
  - **视觉引导：** 用户通过 k3 选择调时还是调分，对应的个位数码管闪烁，提示当前正在调节的是哪个字段。

  **`sec_ten`（第 161 行）：** 秒十位直接输出。

  **`sec_one`（第 162 行）：** 秒个位通过 `seg7()` 函数转换为七段码后再输出。这是整个设计中唯一进行 BCD→七段译码的地方。

### 第 168–175 行：七段译码函数
```verilog
// =========================================================================
// 七段译码纯组合逻辑函数
// =========================================================================
function [6:0] seg7;
    input [3:0] n;
    begin
        case(n)
            0: seg7=7'b0111111; 1: seg7=7'b0000110;
            2: seg7=7'b1011011; 3: seg7=7'b1001111;
            4: seg7=7'b1100110; 5: seg7=7'b1101101;
            6: seg7=7'b1111101; 7: seg7=7'b0000111;
            8: seg7=7'b1111111; 9: seg7=7'b1101111;
            default: seg7=7'b0000000;
        endcase
    end
endfunction
```
- **责任人：丁**
- **说明：** Verilog `function` 实现的七段数码管译码器：

  **函数签名：**
  - `function [6:0] seg7` — 返回 7 位宽，对应七段数码管的 a-g 七个段
  - `input [3:0] n` — 输入 4 位 BCD 码（0-9）

  **译码映射（共阴极数码管，高电平点亮）：**

  | 输入 | 输出 (gfedcba) | 显示 |
  |------|---------------|------|
  | 0 | 0111111 → 段 g 灭，其余亮 | 0 |
  | 1 | 0000110 → 段 b、c 亮 | 1 |
  | 2 | 1011011 | 2 |
  | 3 | 1001111 | 3 |
  | 4 | 1100110 | 4 |
  | 5 | 1101101 | 5 |
  | 6 | 1111101 | 6 |
  | 7 | 0000111 | 7 |
  | 8 | 1111111 — 全亮 | 8 |
  | 9 | 1101111 | 9 |
  | default | 0000000 — 全灭 | 空白 |

  **设计说明：**
  - 采用 `case` 而非 `casex`/`casez`，要求精确匹配，减少意外综合结果。
  - `default` 分支处理非法输入（大于 9 的 BCD 码），显示全灭，安全且符合直觉。

### 第 177 行：模块结束
```verilog
endmodule
```
- **责任人：甲**
- **说明：** `digital_clock` 模块定义结束。所有四人代码组合在一起，构成了一个完整的数字钟系统。

---

## 四人协作总结

| 模块 | 行号 | 人 | 核心贡献 |
|------|------|----|---------|
| 端口与变量声明 | 1–31 | **甲** | 定义模块对外接口和内部寄存器架构 |
| 按键消抖与辅助信号 | 33–48 | **乙** | 保证按键只触发一次、生成闪烁时钟、驱动蜂鸣器 |
| 主时序逻辑 | 52–127 | **丙** | 计时进位、闹钟触发/关闭、按键调时/调闹钟 |
| 显示选择与译码 | 130–176 | **丁** | 三路 MUX 选择显示源、闪烁效果、BCD→七段码 |

### 信号流图

```
clk ──▶ [乙] 消抖 ──ke──▶ [丙] 按键调时 ──▶ 寄存器更新
       [乙] 闪烁 ──blk─▶ [丁] 闪烁效果 ──▶ 数码管
                           [丁] MUX 选择 ──▶ hour_ten/one
rst ──▶ [甲/丙] 复位 ──▶ 所有寄存器初始化     min_ten/one
                           [丁] seg7 译码 ──▶ sec_one
                           [乙] 蜂鸣器驱动 ──▶ buzzer
```

### TEC-8 实验箱引脚映射建议

| 模块端口 | 建议接实验箱位置 |
|----------|----------------|
| clk | 时钟源 → 分频器 → 此引脚 |
| rst | 全局复位按键 |
| key_mode[1:0] | 2 位拨码开关 |
| key_add | 单脉冲按键 |
| k3 | 单刀开关（上=调分，下=调时） |
| hour_ten[3:0] ~ sec_ten[3:0] | 6 个数码管的位选/段选 |
| sec_one[6:0] | 第 6 个数码管的段选（已译码） |
| buzzer | 蜂鸣器驱动引脚 |
